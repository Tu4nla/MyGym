name: Publish D10
on:
  push:
    branches: [handbook/d10-deep-link-compose]
permissions:
  contents: write
jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: handbook/d10-deep-link-compose
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/publish_d10.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add android-learning/data/lessons/d/d10.json android-learning/data/catalog.json android-learning/data/search-index.json android-learning/data/book-plan.json validation-d10.json
          git diff --cached --quiet || git commit -m "docs(android-learning): publish D10 content and metadata"
          git push origin HEAD:handbook/d10-deep-link-compose