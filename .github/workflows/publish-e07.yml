name: Publish E07
on:
  push:
    branches: [handbook/e07-broadcastreceiver-pendingintent]
permissions:
  contents: write
jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: handbook/e07-broadcastreceiver-pendingintent
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/publish_e07.py
      - run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add android-learning/data/lessons/e/e07.json android-learning/data/catalog.json android-learning/data/search-index.json android-learning/data/book-plan.json validation-e07.json
          git diff --cached --quiet || git commit -m "docs(android-learning): publish E07 content and metadata"
          git push origin HEAD:handbook/e07-broadcastreceiver-pendingintent