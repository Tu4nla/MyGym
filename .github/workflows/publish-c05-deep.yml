name: Publish C05 Deep

on:
  push:
    branches:
      - handbook/c05-channel-deep
    paths:
      - android-learning/data/lessons/c/c05.json
      - scripts/publish_c05_deep.py
      - .github/workflows/publish-c05-deep.yml
  pull_request:
    branches:
      - main
    types: [opened, synchronize]
    paths:
      - android-learning/data/lessons/c/c05.json
      - scripts/publish_c05_deep.py
      - .github/workflows/publish-c05-deep.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: handbook/c05-channel-deep
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Validate depth and generate metadata
        run: python scripts/publish_c05_deep.py
      - name: Commit generated metadata and report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add android-learning/data/catalog.json android-learning/data/book-plan.json android-learning/data/search-index.json validation-c05.json
          if git diff --cached --quiet; then
            echo "No generated changes"
          else
            git commit -m "docs(android): publish validated C05"
            git pull --rebase origin handbook/c05-channel-deep
            git push origin HEAD:handbook/c05-channel-deep
          fi
