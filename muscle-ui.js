(()=>{const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function nameMap(){const m={};Object.entries(window.G?.exercises||{}).forEach(([id,d])=>m[d[0]]=id);Object.entries(window.GR?.catalog||{}).forEach(([id,d])=>m[d[0]]=id);return m}function renderMuscles(){const names=nameMap();document.querySelectorAll('.card').forEach(card=>{if(card.querySelector('.muscle-groups'))return;const title=card.querySelector('.card-head h3')?.textContent?.trim();const id=names[title];const muscles=window.GM?.[id];if(!muscles)return;const box=document.createElement('section');box.className='muscle-groups';box.innerHTML=`<div class="muscle-row"><b>Cơ chính</b><div>${(muscles.primary||[]).map(x=>`<span class="muscle primary">${esc(x)}</span>`).join('')}</div></div><div class="muscle-row"><b>Cơ phụ</b><div>${(muscles.secondary||[]).map(x=>`<span class="muscle secondary">${esc(x)}</span>`).join('')}</div></div>`;const head=card.querySelector('.card-head');head?.insertAdjacentElement('afterend',box)})}const observer=new MutationObserver(renderMuscles);observer.observe(document.body,{childList:true,subtree:true});renderMuscles()})();